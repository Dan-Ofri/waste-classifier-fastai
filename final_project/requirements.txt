# ========================
# Core Libraries - ספריות ליבה
# ========================
torch>=2.0.0              # PyTorch - הספרייה המרכזית לבניית רשתות עמוקות
torchvision>=0.15.0       # כלים לעבודה עם תמונות ב-PyTorch (datasets, transforms, models)
numpy>=1.24.0             # חישובים מתמטיים ומערכי מספרים
pillow>=9.5.0             # עיבוד תמונות בפייתון

# ========================
# Data Science & Visualization - ניתוח נתונים וויזואליזציה
# ========================
pandas>=2.0.0             # עבודה עם טבלאות נתונים
matplotlib>=3.7.0         # יצירת גרפים ותרשימים
seaborn>=0.12.0           # גרפים מתקדמים ויפים יותר
scikit-learn>=1.3.0       # כלים למדידת ביצועים (confusion matrix, metrics)

# ========================
# Progress Bars & Logging - בארים והדפסות
# ========================
tqdm>=4.65.0              # progress bars - בארים של התקדמות באימון

# ========================
# Jupyter Notebooks
# ========================
jupyter>=1.0.0            # סביבת Jupyter
ipykernel>=6.25.0         # Kernel לריצת קוד Python בנוטבוקים
ipywidgets>=8.0.0         # ווידג'טים אינטראקטיביים בנוטבוקים

# ========================
# Optional but Recommended - אופציונלי אבל מומלץ
# ========================
tensorboard>=2.13.0       # ויזואליזציה מתקדמת של תהליך האימון (אופציונלי)

# ========================
# הערות התקנה:
# ========================
# להתקנת כל הספריות, הרץ בטרמינל:
# pip install -r requirements.txt
#
# אם יש לך GPU (NVIDIA), וודא שהתקנת את torch עם תמיכת CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# (cu118 = CUDA 11.8, התאם לגרסת ה-CUDA שלך)
